@model IEnumerable<TestG.Models.NhaCungCap>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/adminarea/Views/Shared/_LayoutAdmi.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Tên
        </th>
        <th>
           Số điện thoại
        </th>
        <th>
           Email
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoDienThoai)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.IDNhaCungCap }) |
            @Html.ActionLink("Xem", "Details", new { id=item.IDNhaCungCap }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.IDNhaCungCap })
        </td>
    </tr>
}

</table>
